{
    "height": 35, // Waybar height (to be removed for auto height)
    "layer": "top", // Waybar at top layer
    "margin-top": 5,
    "margin-right": 5,
    "margin-left": 5,
    "spacing": 20,
    "modules-left": ["custom/launcher", "hyprland/workspaces" ],
    "modules-center": ["custom/spotify"],
    "modules-right": ["tray", "wireplumber", "clock"],
    "hyprland/workspaces" : {
        "format" : "{icon}",
        "on-click" : "activate",
        "format-icons" : {
          "urgent": "",
          "active": "",
          "default": " "
        },
        "persistent-workspaces" : {
          "*": 9 // 9 workspaces by default on every monitor
        },
    },
    "hyprland/window": {
        "format": "{}"
    },
    "tray": {
        "spacing": 10
    },
    "clock": {
        "locale": "es_ES.utf8",
        "format": "󰥔  {:L%d/%m %A %H:%M:%S}",
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>",
        "interval": 1
    },
    "wireplumber": {
        "format": "{icon}  {volume}%",
        "format-muted": "󰖁",
        "on-click": "pavucontrol &",
        "format-icons": ["", "", ""]
    },
   "custom/spotify": {
        "format": " {}",
        "return-type": "json",
        "exec": "bash ~/.config/waybar/scripts/mediaplayer.sh spotify 2> /dev/null",
        "exec-if": "pgrep spotify",
        "interval": 5,
        "on-click": "playerctl play-pause",
        "on-double-click-right": "playerctl next",
        "on-scroll-down": "playerctl previous",
    },
    "custom/launcher": {
        "format": "",
        "tooltip" : false,
        "on-click": "rofi -show drun &",
    },
}
